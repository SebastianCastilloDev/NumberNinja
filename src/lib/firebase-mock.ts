// ==========================================================================
// CONFIGURACIÓN DE FIREBASE - MODO DEMO
// Configuración temporal desactivada para usar servicios Mock
// ==========================================================================

// Solo exportar valores null para evitar errores de inicialización
// cuando no tenemos credenciales reales configuradas

console.log("🚧 Firebase temporalmente desactivado - usando modo Mock");

export const db = null;
export const auth = null;

const firebaseMockConfig = null;
export default firebaseMockConfig;
